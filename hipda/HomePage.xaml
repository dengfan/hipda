<Page
    x:Class="hipda.HomePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:hipda"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="WhiteSmoke"
    RequestedTheme="Light">

    <!--<Page.Transitions>
        <TransitionCollection>
            <NavigationThemeTransition>
                <NavigationThemeTransition.DefaultNavigationTransitionInfo>
                    <CommonNavigationTransitionInfo IsStaggeringEnabled="True"/>
                </NavigationThemeTransition.DefaultNavigationTransitionInfo>
            </NavigationThemeTransition>
        </TransitionCollection>
    </Page.Transitions>-->
    
    <Page.Resources>
        <CollectionViewSource x:Name="cvsForumGroups" IsSourceGrouped="True" ItemsPath="Forums"/>

        <local:BackgroundStyleSelecterForForumItem x:Key="BackgroundStyleSelecterForForumItem"/>
        <local:ListGroupStyleSelector x:Key="listGroupStyleSelector"/>
        
        <Style x:Key="GroupItemStyle" TargetType="ListViewItem">
            <Setter Property="Padding" Value="10" />
            <Setter Property="BorderThickness"  Value="{StaticResource AppBarBottomBorderThemeThickness}"/>
            <Setter Property="BorderBrush" Value="LightGray"/>
        </Style>
        
        <DataTemplate x:Key="GroupItemTemplate">
            <StackPanel>
                <StackPanel Orientation="Horizontal">
                    <TextBlock
                        Text="{Binding Name}"
                        TextWrapping="WrapWholeWords"
                        Style="{ThemeResource BodyTextBlockStyle}">
                    </TextBlock>
                    <TextBlock
                        Text="{Binding TodayQuantity}"
                        TextWrapping="WrapWholeWords"
                        Style="{ThemeResource BaseTextBlockStyle}"
                        Foreground="{ThemeResource PhoneAccentBrush}"
                        FontWeight="Bold">
                    </TextBlock>
                </StackPanel>
                
                <TextBlock
                    Text="{Binding Info}"
                    TextWrapping="WrapWholeWords"
                    Style="{ThemeResource ListViewItemSubheaderTextBlockStyle}"/>
            </StackPanel>
        </DataTemplate>
    </Page.Resources>

    <Grid>
        <Pivot x:Name="Pivot"
               CommonNavigationTransitionInfo.IsStaggerElement="True">
            <Pivot.HeaderTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding}" FontSize="{StaticResource TextStyleExtraExtraLargeFontSize}" />
                </DataTemplate>
            </Pivot.HeaderTemplate>
            <PivotItem
                Header="版区" 
                CommonNavigationTransitionInfo.IsStaggerElement="True"
                Margin="0">
                <ListView
                    Padding="10,0,10,0"
                    ItemsSource="{Binding Source={StaticResource cvsForumGroups}}"
                    IsItemClickEnabled="True"
                    ItemClick="ForumItem_ItemClick"
                    ContinuumNavigationTransitionInfo.ExitElementContainer="True"
                    ItemTemplate="{StaticResource GroupItemTemplate}"
                    GroupStyleSelector="{StaticResource ResourceKey=listGroupStyleSelector}"
                    ItemContainerStyle="{StaticResource GroupItemStyle}"/>
            </PivotItem>
            <PivotItem 
                Header="个人中心" 
                CommonNavigationTransitionInfo.IsStaggerElement="True"
                Margin="0">
                <StackPanel Margin="10,0,10,0" Orientation="Vertical">
                    <Grid x:Name="loginPanel">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="auto"/>
                            <RowDefinition Height="auto"/>
                            <RowDefinition Height="auto"/>
                        </Grid.RowDefinitions>

                        <TextBox Grid.Row="0" x:Name="usernameTextBox" TabIndex="1" Header="用户名" Background="White" BorderBrush="Black"/>
                        <PasswordBox Grid.Row="1" x:Name="passwordTextBox" TabIndex="2" Header="密码" Background="White" BorderBrush="Black" />
                        <Button Grid.Row="2" x:Name="loginButton" TabIndex="3" Click="loginButton_Click" Height="60" Content="登录" HorizontalAlignment="Stretch" BorderBrush="Green" Background="Green" Foreground="White" />
                    </Grid>
                </StackPanel>
            </PivotItem>
        </Pivot>
        <ProgressBar x:Name="replyProgressBar" IsIndeterminate="True" Visibility="Collapsed" />
    </Grid>

    <Page.BottomAppBar>
        <CommandBar ClosedDisplayMode="Minimal">
            <AppBarButton x:Name="ReplyButton" Icon="OtherUser" Label="登录">
                <!--<AppBarButton.Flyout>
                    <Flyout>
                        
                    </Flyout>
                </AppBarButton.Flyout>-->
            </AppBarButton>
            <!--<CommandBar.SecondaryCommands>
                <AppBarButton x:Name="SecondaryButton2" Icon="Read" Label="个人中心" />
            </CommandBar.SecondaryCommands>-->
        </CommandBar>
    </Page.BottomAppBar>
</Page>
